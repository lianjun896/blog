# 🍌 治愈系留言板

一个纯静态的治愈系留言板小站，登录后即可留言，支持 **左上角用户信息卡片**（头像 + 昵称 + 个性签名，点击可查看完整个人资料）。

> 本网站为**纯静态站点**：没有后端，所有数据都以 **JSON 文件**的形式存放在项目里，可直接部署到 **GitHub Pages** 免费运行。

---

## ✨ 功能

- 账号密码登录（账号存于 `users.json`）
- 登录后顶部**左上角**显示当前用户信息卡片（仿微信/QQ：头像在左、昵称在右、下方个性签名）
- 点击左上角卡片打开「个人资料」弹窗，展示性别、地区、生日、星座、邮箱、职业、爱好、个人简介、个性标签等
- **留言列表显示每位作者的真实头像**，点击任意留言的头像即可弹出**该作者的个人资料**（没有头像显示首字母，没有资料则显示默认信息）
- 资料字段**数据驱动**：往 `user_profiles.json` 里加字段，页面自动显示，无需改代码
- 头像支持任意图片文件（png / jpg / gif / ico / webp），加载失败自动回退为首字母彩色圆头像
- 留言：保存后自动生成完整 `messages.json` 内容并复制到剪贴板，粘贴回文件即可永久保存

---

## 📁 文件结构

| 文件 | 作用 | 谁维护 |
|---|---|---|
| `index.html` | 页面结构（登录页 + 留言板 + 资料弹窗） | 一般不用动 |
| `app.js` | 页面逻辑（登录、留言、资料渲染） | 一般不用动 |
| `login.css` / `blog.css` | 样式 | 一般不用动 |
| `users.json` | **账号配置**（登录账号密码） | 你直接改 |
| `user_profiles.json` | **个人资料配置**（头像、昵称、签名、性别等） | 你直接改 |
| `messages.json` | **留言内容** | 页面生成/你粘贴 |
| `LJ.ico` | 示例头像图片 | 可替换 |
| `text.jpg` | 页面背景图 | 可替换 |
| `TERMS.md` | 使用条款与免责声明（页面登录前须知同步展示） | 你 |
| `README.md` | 使用说明 | 你 |

---

## ✏️ 如何修改数据

### 1. 修改账号（`users.json`）

```jsonc
{
    "username": "LJ",        // 账号
    "password": "lj123456"   // 密码
}
```

- 想加账号：照样式再写一组 `{ "username": "xxx", "password": "yyy" }`。
- 此文件支持 `//` 注释，方便写说明。

### 2. 修改个人资料（`user_profiles.json`）

```jsonc
{
    "username": "LJ",            // 必须与 users.json 里的账号一致
    "nickname": "LJ",            // 昵称
    "avatar": "LJ.ico",          // 头像图片文件名
    "signature": "生活明朗，万物可爱~", // 个性签名
    "gender": "男",              // 性别
    "region": "广东 · 佛山",       // 地区
    "hobby": "音乐、摄影、旅行",    // 爱好
    "tags": ["治愈系", "音乐"]     // 个性标签
}
```

- **更换头像**：把新图片文件放到项目根目录（或 `avatars/` 文件夹），然后把 `avatar` 改成该文件名，例如 `"avatar": "avatars/我的头像.png"`。
- **增加新资料项**：直接在对应对象里加一行，例如：
  ```jsonc
  "school": "某某大学",   // 新增字段，页面资料弹窗会自动显示
  ```
- 此文件支持 `//` 注释，每个字段后都有中文说明。

### 3. 修改/添加留言（`messages.json`）

```json
[
    {
        "username": "LJ",
        "content": "留言内容",
        "time": "2026年08月31日 10:28:38"
    }
]
```

- 在网站上发布新留言后，会自动弹出完整的新版 `messages.json` 内容并复制到剪贴板，**把复制的内容完整粘贴回 `messages.json` 即可永久保存**。
- 你也可以直接手动编辑该文件（例如删除某条留言、修改内容）。
- ⚠️ `messages.json` 是纯 JSON（不含注释），因为它是页面自动生成的。

### 通用注意事项（JSONC）

- `users.json` 和 `user_profiles.json` 支持 `//` 和 `/* */` 注释；
- 字符串必须用**双引号** `" "`；
- 同一对象**最后一个字段后面不要加逗号**；
- 留空的字段（如 `""`）在资料弹窗里会自动隐藏，填了才显示。

---

## 🚀 本地预览

> ⚠️ 不要直接双击打开 `index.html`。因为登录、留言需要读取 JSON 文件，浏览器会拦截 `file://` 下的请求，请用本地服务器方式预览。

在项目目录执行任意一种：

```bash
# 方式一：Python（推荐）
python -m http.server 8080

# 方式二：VS Code 安装 Live Server 插件后，右键 index.html → Open with Live Server
```

然后浏览器访问 `http://localhost:8080`。

---

## 🌐 部署到 GitHub Pages

1. 在 GitHub 新建一个仓库，把本项目的所有文件上传（或 `git push`）。
2. 打开仓库 **Settings → Pages**。
3. 在 **Source** 选择：
   - **Deploy from a branch** → 选择分支 `main`，目录选 `/ (root)`，点 Save。
4. 等 1~2 分钟，访问 `https://你的用户名.github.io/仓库名/` 即可。

> 已包含 `.nojekyll` 文件，GitHub Pages 不会对站点做额外处理，静态资源可正常访问。

---

## ⚖️ 法律声明（免责声明 / 使用条款）

本网站已随附《使用条款与免责声明》（完整版），并展示在**登录页（访问前须知）**和**留言板页脚**中，点击即可查看完整内容。条款重点涵盖：

- **免费网站声明**：本站为免费、非商业、个人静态网站，**不涉及任何资金往来**；任何以本站名义索要金钱、转账的行为均为诈骗；
- **明文存储与安全风险（重点）**：所有账号、密码、个人资料、留言均以**明文公开存储**于 JSON 文件中，不具备真实安全性；登录仅为前端演示，不构成真实认证；请勿输入真实密码或敏感个人信息；
- 其他安全风险：静态托管中断、爬取滥用、第三方链接等；
- 用户发布内容的法律责任与禁止发布的违法和不良信息（依据《网络安全法》《网络信息内容生态治理规定》等）；
- 个人信息与隐私说明（依据《个人信息保护法》）、免责条款、知识产权、未成年人保护、法律适用与争议解决。

> 完整文本见 `TERMS.md`。如需修改，请同步更新 `TERMS.md` 与页面弹窗（`index.html` 中 `#termsModal` 部分）。

---

## 🔐 说明

- 由于是纯静态站点，登录校验在**浏览器端**完成，密码以明文形式存于 `users.json`，适合个人/朋友间的小站；请勿存放重要敏感信息，也不要用它做真实安全认证。
